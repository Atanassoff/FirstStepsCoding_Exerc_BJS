function demo(input){
    let a = Number(input[0]); 
    let b = Number(input[1]);
    let h = Number(input[2]);
    let percent = Number(input[3]);
    let volume = a * b * h;
    let litres = volume / 1000;
    let total = litres - (litres * percent / 100);
    console.log(total);
}
