function demo(input){
    let chicken = Number(input[0]) * 10.35; 
    let fish = Number(input[1]) * 12.4;
    let veg = Number(input[2]) * 8.15;
    let dessert = (chicken + fish + veg) * 0.2;
    let total = chicken + fish + veg + dessert + 2.5;
    console.log(total);
}
