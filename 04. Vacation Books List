function demo(input){
    let pages = Number(input[0]);
    let pagesAnHour = Number(input[1]);
    let days = Number(input[2]);
    let hours = pages / pagesAnHour;
    let hoursADay = hours / days;
    console.log(hoursADay);
}
